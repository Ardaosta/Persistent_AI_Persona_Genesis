# Engine pack: Grok (xAI) — RESEARCH-NEEDED STUB

**Tier:** candidate — recommend ONLY if its agentic tool-use is genuinely solid. This is the one to verify rather than assume: confirm Grok does reliable multi-step native tool use before recommending it as a Companion engine (a chat-strong model that can't act dependably is not a fit).

Both modes are unspecified. Before writing this pack, research and map (grounded): Grok's API native tool-calling format and reliability (for a Mode A `GrokBackend`), whether any agentic harness exists for Mode B, its boot-context and memory conventions, and its footprint. If multi-step tool use proves unreliable, mark Grok "works via Mode A, not recommended" and stop. Do not fabricate.
