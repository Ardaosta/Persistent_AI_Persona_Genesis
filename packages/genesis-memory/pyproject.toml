[project]
name = "genesis-memory"
version = "0.0.1"
description = "Genesis memory subsystem: the canonical markdown vault, lean index, and blessed write path."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/genesis_memory"]
